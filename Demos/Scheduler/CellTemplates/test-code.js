/*

new Promise(resolve => {
    var i = 0;
    const interval = setInterval(() => {
        if(i > 50 || $('.dx-scheduler-appointment-title').length) {
            clearInterval(interval);
            resolve();
        }
        i++;
    }, 100);
});

*/
