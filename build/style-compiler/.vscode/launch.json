{
  "version": "0.2.0",
  "configurations": [
    {
      "name": ".NET Core Launch (console)",
      "type": "coreclr",
      "request": "launch",
      "preLaunchTask": "build",
      "program": "${workspaceRoot}/bin/style-compiler.dll",
      "args": [ "TODO" ],
      "cwd": "${workspaceRoot}",
      "externalConsole": true
    }
  ]
}
