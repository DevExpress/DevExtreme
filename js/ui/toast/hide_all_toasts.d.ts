import { UserDefinedElement } from '../../core/element';

/**
 * @docid ui.hideAllToasts
 * @static
 * @publicName hideAllToasts(container)
 * @param1 container:string|Element|jQuery|undefined
 * @public
 */
 export default function hideAllToasts(container?: string | UserDefinedElement): void;
