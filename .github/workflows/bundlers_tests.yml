name: Bundlers tests

on: [push, pull_request]

jobs:
  bundler-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
    - name: Get sources
      uses: actions/checkout@v2

    - name: Use Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '15'

    - name: Run npm install
      run: npm install --no-package-lock --no-audit --no-fund

    - name: Build
      run: npm run build
    
    - name: Run npm insatall in test directory
      working-directory: ./testing/test_bundlers  
      run: npm install

    - name: Install DevExtreme in test directory
      working-directory: ./testing/test_bundlers
      run: npm install ../../artifacts/npm/devextreme/    

    - name: Create js bundle with webpack (CJS import)
      working-directory: ./testing/test_bundlers
      run: npm run build:webpack-cjs

    - name: Create js bundle with webpack
      working-directory: ./testing/test_bundlers
      run: npm run build:webpack-esm

    - name: Create js bundle with rollup
      working-directory: ./testing/test_bundlers
      run: npm run build:rollup-esm

    - name: Create js bundle with parcel
      working-directory: ./testing/test_bundlers
      run: npm run build:parcel-esm

    - name: Create js bundle with browserify
      working-directory: ./testing/test_bundlers
      run: npm run build:browserify-cjs
