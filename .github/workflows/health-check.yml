name: Health Check

on:
  workflow_dispatch:
  push:
    branches:
      - 26_1

jobs:
  invoke:
    name: Invoke
    uses: DevExpress/github-actions/.github/workflows/repository-check.yml@repo-check/latest
