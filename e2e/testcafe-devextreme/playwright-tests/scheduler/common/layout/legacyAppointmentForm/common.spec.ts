import { test } from '@playwright/test';

test.describe('AppointmentForm screenshot tests', () => {
  test.skip('Appointment form tests', async () => {
    // TODO: needs Scheduler page object (legacyAppointmentPopup)
  });
});
