import themesDefault from '../__internal/ui/themes';

export const {
    setDefaultTimeout,
    init,
    initialized,
    resetTheme,
    ready,
    waitWebFont,
    isWebFontLoaded,
    isCompact,
    isDark,
    isGeneric,
    isMaterial,
    isFluent,
    isMaterialBased,
    detachCssClasses,
    attachCssClasses,
    current,
    waitForThemeLoad,
    isPendingThemeLoaded,
} = themesDefault;

export default themesDefault;
