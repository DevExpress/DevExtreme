{
  "$schema": "https://json-schema.org/schema",
  "title": "Prepare Submodules Executor Schema",
  "description": "Prepare submodule entry points with package.json files",
  "type": "object",
  "properties": {
    "distDirectory": {
      "type": "string",
      "description": "Distribution directory containing ESM and CJS builds. This directory will be scanned to generate submodule package.json files.",
      "default": "./npm"
    }
  }
}
