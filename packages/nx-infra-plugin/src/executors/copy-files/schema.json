{
  "type": "object",
  "properties": {
    "files": {
      "type": "array",
      "description": "Files to copy (array of {from, to})",
      "items": {
        "type": "object",
        "properties": {
          "from": {
            "type": "string"
          },
          "to": {
            "type": "string"
          }
        },
        "required": ["from", "to"]
      }
    }
  },
  "required": ["files"]
}
