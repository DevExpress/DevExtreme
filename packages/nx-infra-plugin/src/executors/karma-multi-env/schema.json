{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "title": "Karma Multi-Environment Test Executor",
  "properties": {
    "karmaConfig": {
      "type": "string",
      "description": "Path to karma.conf.js"
    },
    "environments": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["client", "server", "hydration"]
      },
      "default": ["client", "server", "hydration"]
    },
    "watch": {
      "type": "boolean",
      "default": false
    },
    "debug": {
      "type": "boolean",
      "default": false
    },
    "timeout": {
      "type": "number",
      "description": "Test timeout in milliseconds",
      "default": 300000
    }
  },
  "required": ["karmaConfig"]
}
