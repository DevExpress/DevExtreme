{
  "$schema": "https://json-schema.org/schema",
  "type": "object",
  "properties": {
    "files": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Paths to files to compress (relative to project root)"
    },
    "mode": {
      "type": "string",
      "enum": ["minify", "beautify", "strip-debug", "normalize"],
      "description": "Compression mode"
    },
    "eulaUrl": {
      "type": "string",
      "description": "EULA URL to preserve in comments during compression"
    },
    "exclude": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Glob patterns for files to exclude from compression (relative to project root)"
    }
  },
  "required": ["files", "mode"]
}
