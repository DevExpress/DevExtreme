{
  "tasksRunnerOptions": {
    "default": {
      "runner": "nx/tasks-runners/default",
      "options": {
        "cacheableOperations": [
          "build",
          "watch"
        ]
      }
    }
  },
  "targetDefaults": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "{projectRoot}/packages/{angular, core, react}/{lib, dist, tsc-out}"
      ]
    },
    "watch": {
      "outputs": [
        "{projectRoot}/packages/{angular, core, react}/{lib, dist, tsc-out}"
      ]
    },
    "start": {
      "dependsOn": ["^build"]
    }
  },
  "defaultBase": "22_2"
}
